/**
 * Слой аутентификации. Реализуется на Этапе 1.
 *
 * Устройство (утверждено архитектурой v2, с поправкой "Supabase Auth в v1"):
 * - core/      — сессии, guards, permission-карта. Не знает о провайдере.
 * - providers/ — адаптеры входа. v1: Supabase Auth (+ Telegram Login поверх).
 *                Импорт @supabase/* допустим только здесь и в storage/adapters.
 *
 * Таблица auth_identities (Этап 1) сохраняется несмотря на Supabase Auth —
 * это гарантия замены провайдера без миграции пользователей.
 */
export {};
